#ifndef NANDA_INDEX_TYPES_HEADER
#define NANDA_INDEX_TYPES_HEADER

#include <array>
#include <cstddef>

namespace nanda {

using index_type = int;
using size_type = std::size_t;

// template<size_type N>
// using md_idx = std::array<index_type, N>;

} // namespace nanda

#endif